let Subject;_328‍.x([["remove",()=>remove],["removeAPI",()=>removeAPI]]);_328‍.w("../../class/subject",[["Subject",["Subject"],function(v){Subject=v}]]);

       const remove = async (id, update) => {
	try {
		const removedSubject = await Subject.findByIdAndDelete(id);
		if (removedSubject) {
			return removedSubject;
		} else {
			throw new Error('Matéria não econtrada');
		}
	} catch (e) {
		throw new Error('Não foi possível buscar por essa matéria');
	}
};

       const removeAPI = async (req, res) => {
	const removedSubject = await remove(req.params.id);
	res.status(200).json(removedSubject);
};
