let Subject;_87e‍.x([["updateSubject",()=>updateSubject],["updateSubjectAPI",()=>updateSubjectAPI]]);_87e‍.w("../../class/subject",[["Subject",["Subject"],function(v){Subject=v}]]);

       const updateSubject = async (id, update) => {
	try {
		const updatedSubject = await Subject.findByIdAndUpdate(id, update);
		if (updatedSubject) {
			return updatedSubject;
		} else {
			throw new Error('Matéria não econtrada');
		}
	} catch (e) {
		throw new Error('Não foi possível buscar por essa matéria');
	}
};

       const updateSubjectAPI = async (req, res) => {
	const updatedSubject = await updateSubject(req.params.id, req.body);
	res.status(200).json(updatedSubject);
};
