let Mark;_821‍.x([["create",()=>create],["add",()=>add],["addAPI",()=>addAPI]]);_821‍.w("../../class/mark",[["Mark",["Mark"],function(v){Mark=v}]]);

       const create = async (data) => {
	try {
		const newMark = await new Mark(data).save();
		return newMark;
	} catch (e) {
		throw new Error('Não foi possível criar');
	}
};

       const add = async (created) => {};

       const addAPI = async (req, res) => {
	const { subject, value, weight, title, description } = req.body;

	const data = { subject, value, weight, title, description };

	const created = await create(data);
	res.status(201).json(created);

	const added = await add(created);
	res.status(201).json(added);
};
