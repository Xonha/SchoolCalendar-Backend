let Subject;_a86‍.x([["update",()=>update],["updateAPI",()=>updateAPI]]);_a86‍.w("../../class/subject",[["Subject",["Subject"],function(v){Subject=v}]]);

       const update = async (id, update) => {
	try {
		const updatedSubject = await Subject.findByIdAndUpdate(id, update);
		if (updatedSubject) {
			return updatedSubject;
		} else {
			throw new Error('Matéria não econtrada');
		}
	} catch (e) {
		throw new Error('Não foi possível buscar por essa matéria');
	}
};

       const updateAPI = async (req, res) => {
	const updatedSubject = await update(req.params.id, req.body);
	res.status(200).json(updatedSubject);
};
