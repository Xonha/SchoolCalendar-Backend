let Event;_7c5‍.x([["deleteEvent",()=>deleteEvent],["deleteSubjectAPI",()=>deleteSubjectAPI]]);_7c5‍.w("../../class/event",[["Event",["Event"],function(v){Event=v}]]);

       const deleteEvent = async (id) => {
	try {
		const deletedEvent = await Event.findByIdAndDelete(id);
		if (deletedEvent) {
			return deletedEvent;
		} else {
			throw new Error('Evento não econtrada');
		}
	} catch (e) {
		throw new Error('Não foi possível buscar por esse evento');
	}
};

       const deleteSubjectAPI = async (req, res) => {
	const deletedEvent = await deleteEvent(req.params.id);
	res.status(200).json(deletedEvent);
};
