let Subject;_422‍.x([["createSubject",()=>createSubject],["createSubjectAPI",()=>createSubjectAPI]]);_422‍.w("../../class/subject",[["Subject",["Subject"],function(v){Subject=v}]]);

       const createSubject = async (subjectData) => {
	try {
		const newSubject = await new Subject(subjectData).save();
		return newSubject;
	} catch (e) {
		throw new Error('Não foi possível criar matéria');
	}
};

       const createSubjectAPI = async (req, res) => {
	const {
		name,
		color,
		teacherName,
		startDate,
		endDate,
		weekDay,
		comment,
	} = req.body;

	const subjectData = {
		name,
		color,
		teacherName,
		startDate,
		endDate,
		weekDay,
		comment,
		mark: [],
	};

	const createdSubject = await createSubject(subjectData);
	res.status(201).json(createdSubject);
};
