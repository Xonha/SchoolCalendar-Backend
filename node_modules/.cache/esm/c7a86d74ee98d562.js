let Subject;_ee9‍.x([["create",()=>create],["createAPI",()=>createAPI]]);_ee9‍.w("../../class/subject",[["Subject",["Subject"],function(v){Subject=v}]]);

       const create = async (data) => {
	try {
		const newSubject = await new Subject(data).save();
		return newSubject;
	} catch (e) {
		throw new Error('Não foi possível criar');
	}
};

       const createAPI = async (req, res) => {
	const {
		name,
		color,
		teacherName,
		startDate,
		endDate,
		weekDay,
		comment,
	} = req.body;

	const data = {
		name,
		color,
		teacherName,
		startDate,
		endDate,
		weekDay,
		comment,
		mark: [],
	};

	const created = await create(data);
	res.status(201).json(created);
};
