let Subject;_6ee‍.x([["deletedSubject",()=>deletedSubject],["deletedSubjectAPI",()=>deletedSubjectAPI]]);_6ee‍.w("../../class/subject",[["Subject",["Subject"],function(v){Subject=v}]]);

       const deletedSubject = async (id, update) => {
	try {
		const deletedSubject = await Subject.findByIdAndDelete(id);
		if (deletedSubject) {
			return deletedSubject;
		} else {
			throw new Error('Matéria não econtrada');
		}
	} catch (e) {
		throw new Error('Não foi possível buscar por essa matéria');
	}
};

       const deletedSubjectAPI = async (req, res) => {
	const deletedSubject = await deletedSubject(req.params.id);
	res.status(200).json(deletedSubject);
};
